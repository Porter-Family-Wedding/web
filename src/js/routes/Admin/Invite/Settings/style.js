const style = (theme) => ({
  container: {
    padding: theme.spacing(2),
  },
  paper: {
    padding: theme.spacing(2),
    margin: theme.spacing(2),
    borderRadius: theme.spacing(2),
  }
});

export default style;
